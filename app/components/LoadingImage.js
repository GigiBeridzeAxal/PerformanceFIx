"use client"
import Image from "next/image";
import { useData } from "../service/Provider";

const LoadingImage = () => {
  const data = useData();

  return (
    <div>
    
    </div>
  );
};

export default LoadingImage;
